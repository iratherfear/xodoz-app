# spring.application.name=xodos-app

spring:
    application:
        name: xodos-app
    datasource:
        url: jdbc:mysql://localhost:3306/xodoz?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
        driver-class-name: com.mysql.cj.jdbc.Driver
        username: xodoz
        password: password
    jpa:
        show-sql: true
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL8Dialect
